# Автоматический перевод строк локализации

## Описание 

Скрипт `auto_translate.py` автоматически переводит непереведенные строки в файлах локализации, используя Google Translate API. Скрипт:

- ✅ Находит только непереведенные строки (где значение равно английскому)
- ✅ Сохраняет структуру файла, имена переменных и синтаксис Python
- ✅ Пропускает технические строки (эмодзи, константы, пути к файлам)
- ✅ Работает построчно, заменяя только значения строк

## Установка 

Установите необходимую библиотеку:

```bash
pip install deep-translator
```

## Использование

### Базовое использование

Перевести до 20 строк в файле (по умолчанию):

```bash
python auto_translate.py --lang KK
```

### Параметры

- `--lang CODE` - Код языка для перевода (например, `KK`, `FA`, `IT`, `JA`, `TL`, `UK`, `TR`, `TH`, `HA`, `ID`, `UR`, `KO`)
- `--dry-run` - Только показать, что будет переведено, без сохранения
- `--batch N` - Количество переводов за раз (по умолчанию 20)
- `--start-line N` - Начальная строка для обработки (1-based, включительно)
- `--end-line N` - Конечная строка для обработки (1-based, включительно)

### Примеры

```bash
# Перевести 20 строк в казахском файле
python auto_translate.py --lang KK

# Перевести 50 строк за раз
python auto_translate.py --lang KK --batch 50

# Посмотреть, что будет переведено (без сохранения)
python auto_translate.py --lang KK --dry-run

# Перевести только строки с 2106 по 2151
python auto_translate.py --lang KK --start-line 2106 --end-line 2151

# Перевести 1309 строк за раз (большой батч)
python auto_translate.py --lang KK --batch 1309

# Перевести строки в диапазоне с ограничением батча
python auto_translate.py --lang KK --start-line 1000 --end-line 2000 --batch 100
```

### Проверка переводов

После перевода рекомендуется проверить результаты:

```bash
# Проверить статус переводов
python check_translations.py
```

## Как это работает

1. Скрипт загружает `messages_EN.py` как эталон
2. Загружает целевой файл (например, `messages_KK.py`)
3. Сравнивает значения переменных и находит непереведенные
4. Для каждой непереведенной строки:
   - Парсит строку присваивания построчно
   - Извлекает значение строки
   - Переводит через Google Translate
   - Заменяет только значение, сохраняя форматирование
5. Сохраняет изменения в файл

## Что пропускается

Скрипт автоматически пропускает:

- Переменные с суффиксами: `_EMOJI`, `_BUTTON_MSG`, `_INACTIVE_MSG`, `_UNAVAILABLE_MSG`, `_VALUES`, `_FILE_NAME_MSG`
- Строки, содержащие только эмодзи
- Технические константы (только заглавные буквы и подчеркивания)
- Пути к файлам (`users/{user_id}/cookie.txt`)
- Названия сервисов (`youtube`, `tiktok`, `instagram`)
- Лог-сообщения в квадратных скобках

## Ограничения

- Скрипт обрабатывает только простые строки на одной строке (не многострочные в скобках)
- Google Translate API имеет лимиты на количество запросов
- Рекомендуется переводить по 20-50 строк за раз

## Рекомендации

1. Начните с `--dry-run` чтобы посмотреть, что будет переведено
2. Переводите небольшими батчами (20-50 строк)
3. Проверяйте результаты после каждого запуска
4. Для многострочных строк используйте ручной перевод

## Пример вывода

```
🌐 Обработка: messages_KK.py (KK)
    📊 Найдено непереведенных строк: 855
    🔄 Перевод ADMIN_BLOCK_USER_USAGE_MSG (строка 806)... ✅
    🔄 Перевод ADMIN_CANNOT_DELETE_ADMIN_MSG (строка 807)... ✅
    ...
    💾 Сохранено 20 переводов

✅ Переведено строк: 20
💡 Запустите снова для перевода следующих 20 строк
```

